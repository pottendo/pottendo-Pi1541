diff --git a/addon/fatfs/ffconf.h b/addon/fatfs/ffconf.h
index bd65f4c2..be8806d5 100644
--- a/addon/fatfs/ffconf.h
+++ b/addon/fatfs/ffconf.h
@@ -47,7 +47,7 @@
 /  (0:Disable or 1:Enable) Also FF_FS_READONLY needs to be 0 to enable this option. */
 
 
-#define FF_USE_LABEL	0
+#define FF_USE_LABEL	1
 /* This option switches volume label API functions, f_getlabel() and f_setlabel().
 /  (0:Disable or 1:Enable) */
 
@@ -183,7 +183,7 @@
 
 
 #define FF_STR_VOLUME_ID	1
-#define FF_VOLUME_STRS		"SD","USB","USB2","USB3","FD","NVME"
+/*#define FF_VOLUME_STRS		"SD","USB","USB2","USB3","FD","NVME" */
 /* FF_STR_VOLUME_ID switches support for volume ID in arbitrary strings.
 /  When FF_STR_VOLUME_ID is set to 1 or 2, arbitrary strings can be used as drive
 /  number in the path name. FF_VOLUME_STRS defines the volume ID strings for each
diff --git a/include/circle/net/httpclient.h b/include/circle/net/httpclient.h
index 571a7752..60bace3b 100644
--- a/include/circle/net/httpclient.h
+++ b/include/circle/net/httpclient.h
@@ -38,7 +38,9 @@ public:
 
 	THTTPStatus Get (const char *pPath,			// "/file[?name=value[&name=value...]]"
 			 u8	    *pBuffer,			// content will be returned here
-			 unsigned   *pLength);			// in: buffer size, out: content length
+			 unsigned   *pLength,                   // in: buffer size, out: content length
+			 u8* pHeader = 0,
+			 unsigned *pHLen = 0);
 
 	THTTPStatus Post (const char *pPath,			// "/file[?name=value[&name=value...]]"
 			  u8	     *pBuffer,			// content will be returned here
@@ -50,8 +52,9 @@ private:
 			     const char		*pPath,		// may include URL parameters
 			     u8			*pBuffer,	// content will be returned here
 			     unsigned		*pLength,	// in: buffer size, out: content length
-			     const char		*pFormData = 0);// form data for POST or 0
-
+			     const char		*pFormData = 0, // form data for POST or 0
+			     u8* pHeader = 0, // header content will be returned here
+			     unsigned *pHLen = 0); // len allocated for header content
 private:
 	CNetSubSystem *m_pNetSubSystem;
 	CIPAddress     m_ServerIP;
diff --git a/include/circle/net/httpdaemon.h b/include/circle/net/httpdaemon.h
index f41b62e8..f4c8f9be 100644
--- a/include/circle/net/httpdaemon.h
+++ b/include/circle/net/httpdaemon.h
@@ -49,7 +49,8 @@ public:
 					const char  *pFormData, // form data from POST ("" for none)
 				        u8	    *pBuffer,	// copy your content here
 				        unsigned    *pLength,	// in: buffer size, out: content length
-				        const char **ppContentType) = 0; // set this if not "text/html"
+				        const char **ppContentType, // set this if not "text/html"
+					const char **ppHeader = 0) = 0; 
 
 	// overwrite this to implement your own access logging
 	virtual void WriteAccessLog (const CIPAddress	&rRemoteIP,
diff --git a/include/circle/sysconfig.h b/include/circle/sysconfig.h
index 566e6eb7..ee692de0 100644
--- a/include/circle/sysconfig.h
+++ b/include/circle/sysconfig.h
@@ -35,14 +35,14 @@
 // increase this value. The value must be a multiple of 16 KByte.
 
 #ifndef KERNEL_MAX_SIZE
-#define KERNEL_MAX_SIZE		(2 * MEGABYTE)
+#define KERNEL_MAX_SIZE		(8 * MEGABYTE)
 #endif
 
 // KERNEL_STACK_SIZE is the size of the stack set on startup for the
 // main thread.  This must be a multiple of 16 KByte.
 
 #ifndef KERNEL_STACK_SIZE
-#define KERNEL_STACK_SIZE	0x20000
+#define KERNEL_STACK_SIZE	(0x100000 * 4)
 #endif
 
 // HEAP_DEFAULT_NEW defines the default heap to be used for the "new"
@@ -85,7 +85,7 @@
 // multiple of 64. Up to 20 sizes can be defined.
 
 #ifndef HEAP_BLOCK_BUCKET_SIZES
-#define HEAP_BLOCK_BUCKET_SIZES	0x40,0x400,0x1000,0x4000,0x10000,0x40000,0x80000
+#define HEAP_BLOCK_BUCKET_SIZES	0x40,0x400,0x1000,0x4000,0x10000,0x40000,0x80000,0x100000*4,0x100000*20
 #endif
 
 ///////////////////////////////////////////////////////////////////////
@@ -155,7 +155,7 @@
 // single core applications, because this may slow down the system
 // because multiple cores may compete for bus time without use.
 
-//#define ARM_ALLOW_MULTI_CORE
+#define ARM_ALLOW_MULTI_CORE
 
 #endif
 
@@ -273,13 +273,13 @@
 // MAX_TASKS is the maximum number of tasks in the system.
 
 #ifndef MAX_TASKS
-#define MAX_TASKS		20
+#define MAX_TASKS		(4*20)
 #endif
 
 // TASK_STACK_SIZE is the stack size for each task.
 
 #ifndef TASK_STACK_SIZE
-#define TASK_STACK_SIZE		0x8000
+#define TASK_STACK_SIZE		KERNEL_STACK_SIZE
 #endif
 
 // NO_BUSY_WAIT deactivates busy waiting in the EMMC, SDHOST and USB
diff --git a/lib/net/httpclient.cpp b/lib/net/httpclient.cpp
index 29aec901..12345eed 100644
--- a/lib/net/httpclient.cpp
+++ b/lib/net/httpclient.cpp
@@ -46,9 +46,10 @@ CHTTPClient::~CHTTPClient (void)
 	m_pNetSubSystem = 0;
 }
 
-THTTPStatus CHTTPClient::Get (const char *pPath, u8 *pBuffer, unsigned *pLength)
+extern "C" void  mylog(const char *str, ...);
+THTTPStatus CHTTPClient::Get (const char *pPath, u8 *pBuffer, unsigned *pLength, u8 *pHeader, unsigned *pHLen)
 {
-	return Request (HTTPRequestMethodGet, pPath, pBuffer, pLength);
+    return Request (HTTPRequestMethodGet, pPath, pBuffer, pLength, 0, pHeader, pHLen);
 }
 
 THTTPStatus CHTTPClient::Post (const char *pPath, u8 *pBuffer, unsigned *pLength, const char *pFormData)
@@ -61,7 +62,9 @@ THTTPStatus CHTTPClient::Request (THTTPRequestMethod  Method,
 				  const char	     *pPath,
 				  u8		     *pBuffer,
 				  unsigned	     *pLength,
-				  const char	     *pFormData)
+				  const char	     *pFormData,
+				  u8* pHeader,
+				  unsigned *pHLen)
 {
 	// connect to server
 	assert (m_pNetSubSystem != 0);
@@ -134,9 +137,12 @@ THTTPStatus CHTTPClient::Request (THTTPRequestMethod  Method,
 	unsigned nLength = 0;
 	boolean bChunked = FALSE;
 	unsigned long ulBytes = 0;
+	unsigned ulHLen = 0;
 
 	char Buffer[FRAME_BUFFER_SIZE];
 	char Line[HTTP_MAX_REQUEST_LINE];
+	char Line0[HTTP_MAX_REQUEST_LINE];
+	Line0[0] = '\0';
 	int nResult;
 	char *pSavePtr;
 
@@ -159,12 +165,43 @@ THTTPStatus CHTTPClient::Request (THTTPRequestMethod  Method,
 				{
 					if (nChar == 0)		// empty line is end of header
 					{
+					    char *pToken;
 						nState = bChunked ? 2 : 1;
 						nChar = 0;
+						*pHLen = strlen((const char *)pHeader);
+						//mylog("%s: first line = '%s'", __FUNCTION__, Line0);
+						if (   (pToken = strtok_r ((char *) Line0, "/", &pSavePtr)) == 0
+						    || strcmp (pToken, "HTTP") != 0
+						    || (pToken = strtok_r (0, " ", &pSavePtr)) == 0
+						    || (pToken = strtok_r (0, " ", &pSavePtr)) == 0
+						    || strcmp (pToken, "200") != 0)
+						{
+						    char *pEnd;
+						    unsigned long ulStatus;
+						    ulStatus = strtoul (pToken, &pEnd, 10);
+						    if (   pEnd != 0
+							   && *pEnd != '\0')
+						    {
+							ulStatus = HTTPInvalidResponseCode;
+						    }
+						    
+						    delete m_pSocket;
+						    m_pSocket = 0;
+						    //mylog("%s: sending back %d", __FUNCTION__, ulStatus);
+						    return (THTTPStatus) ulStatus;
+						}
 					}
 					else
 					{
+					    ulHLen += strlen(Line) + 2;
+					    if ((*pHLen) > ulHLen)
+					    {
+						strcat((char *)pHeader, Line);
+						strcat((char *)pHeader, "\r\n");
+					    }
 						if (nLine++ == 0)	// first line?
+						    strcat(Line0, Line); // save first Line
+#if 0						
 						{
 							// "HTTP/1.x 200 OK" expected
 							char *pToken;
@@ -190,6 +227,7 @@ THTTPStatus CHTTPClient::Request (THTTPRequestMethod  Method,
 							}
 						}
 						else
+#endif						    
 						{
 							// check for transfer encoding option
 							char *pToken = strtok_r (Line, ": ", &pSavePtr);
diff --git a/lib/net/httpdaemon.cpp b/lib/net/httpdaemon.cpp
index e9a5a458..d19d2607 100644
--- a/lib/net/httpdaemon.cpp
+++ b/lib/net/httpdaemon.cpp
@@ -96,6 +96,8 @@ void CHTTPDaemon::Run (void)
 	}
 }
 
+extern "C" void  mylog(const char *str, ...);
+
 void CHTTPDaemon::WriteAccessLog (const CIPAddress &rRemoteIP, THTTPRequestMethod RequestMethod,
 				  const char *pRequestURI, THTTPStatus Status,
 				  unsigned nContentLength)
@@ -190,13 +192,15 @@ void CHTTPDaemon::Worker (void)
 	const char *pContentType = "text/html";
 
 	const char *pStatusMsg = "OK";
+	const char *pHeader = "";
 
 	if (Status == HTTPOK)
 	{
 		// get content
 		assert (m_pContentBuffer != 0);
 		Status = GetContent (m_RequestPath, m_RequestParams, m_RequestFormData,
-				     m_pContentBuffer, &nContentLength, &pContentType);
+				     m_pContentBuffer, &nContentLength, &pContentType,
+				     &pHeader);
 		assert (nContentLength <= m_nMaxContentSize);
 		assert (pContentType != 0);
 
@@ -251,12 +255,20 @@ void CHTTPDaemon::Worker (void)
 
 	// send HTTP response header
 	CString Header;
-	Header.Format ("HTTP/1.1 %u %s\r\n"
-		       "Server: " SERVER "\r\n"
-		       "Content-Type: %s\r\n"
-		       "Content-Length: %u\r\n"
-		       "Connection: close\r\n"
-		       "\r\n", Status, pStatusMsg, pContentType, nContentLength);
+	if (pHeader[0] == '\0')
+	{
+	    Header.Format ("HTTP/1.1 %u %s\r\n"
+			   "Server: " SERVER "\r\n"
+			   "Content-Type: %s\r\n"
+			   "Content-Length: %u\r\n"
+			   "Connection: close\r\n"
+			   "\r\n", Status, pStatusMsg, pContentType, nContentLength);
+	}
+	else
+	{
+	    mylog("%s: sending alternative header: %s", __FUNCTION__, pHeader);
+	    Header = CString(pHeader);
+	}
 
 	if (m_pSocket->Send ((const char *) Header, Header.GetLength (), MSG_DONTWAIT) < 0)
 	{
diff --git a/lib/usb/usbstring.cpp b/lib/usb/usbstring.cpp
index 54e9bd47..61416a76 100644
--- a/lib/usb/usbstring.cpp
+++ b/lib/usb/usbstring.cpp
@@ -113,6 +113,10 @@ boolean CUSBString::GetFromDescriptor (u8 ucID, u16 usLanguageID)
 		}
 	}
 
+	// accept broken USB sticks
+	if (m_pUSBString->bLength <= 2)
+	    return FALSE;
+
 	// convert into ASCII string
 	assert (m_pUSBString->bLength > 2);
 	assert ((m_pUSBString->bLength & 1) == 0);
